const authorization = require('./authorization');
const error = require('./error');
const validation = require('./validation');

/**
 * This code is a module that exports three other modules: authorization, error, and validation.
 * These modules are imported at the top of the file using the require() function.
 */
module.exports = {
  authorization,
  error,
  validation,
};
